import { Box } from "@mui/material";

const dashboard = () => {
  return (
    <Box
      component="section"
      sx={{ p: 2, border: "1px dashed grey", width: "100%" }}
    >
      dashboard
    </Box>
  );
};

export default dashboard;
